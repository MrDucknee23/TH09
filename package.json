{
  "name": "fullstack-project",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client-admin && npm install --prefix client-customer",
    "build": "npm run build --prefix client-admin && npm run build --prefix client-customer",
    "render-build": "npm run install-all && npm run build",
    "start": "node server/index.js"
  }
}